# OmniMind 마스터 구현 로드맵

> 설계서: `docs/superpowers/specs/2026-07-13-omnimind-design.md` (SSOT)
> 각 단계(P0~P7)는 착수 시점에 상세 플랜(`docs/superpowers/plans/`)을 별도 작성한다.
> 상세 플랜에는 완전한 코드·명령·테스트가 포함된다. 이 문서는 전체 그림과 완료 기준만 담는다.

## 원칙

- 매 단계는 그 자체로 배포 가능한 소프트웨어를 산출한다 (P2부터 실사용자 유입)
- 월 고정비 0원 — 무료 티어만 사용
- 계산 엔진(P1)은 TDD 필수 — 정답이 존재하는 영역
- 모든 사용자 대면 문구는 설계서 §5.4 문체 규칙을 따른다

---

## P0. 기반 구축 — ✅ 완료 (2026-07-14, 머지 커밋 635a852)

**목표:** 로그인까지 되는 빈 앱을 Vercel에 배포
**결과:** https://omni-mind-i6uj.vercel.app — 프로덕션에서 카카오 로그인 → 4탭 이동 검증 완료. 상세 플랜: `2026-07-13-p0-foundation.md`

| # | 태스크 | 산출물 |
|---|--------|--------|
| 1 | Next.js 스캐폴딩 (TS, App Router, Tailwind) | 로컬에서 뜨는 앱 |
| 2 | 디자인 토큰 + 폰트 (Pretendard, Noto Serif KR) | 브랜드 컬러·서체 적용된 전역 스타일 |
| 3 | 앱 셸 — 하단 4탭 네비게이션 + 플레이스홀더 페이지 | 홈/나/고민/마음 이동 가능 |
| 3.5 | 검증 파이프라인 + 테스트 인프라 (Vitest·RTL·GitHub Actions, `npm run verify`) | 커밋 전 필수 검증 체계 |
| 4 | Supabase 연동 (클라이언트/서버 유틸) | DB 연결 확인 |
| 5 | 소셜 로그인 (카카오·구글) + 세션 미들웨어 | 로그인/로그아웃 동작 |
| 6 | Vercel 배포 | 공개 URL |

**완료 기준:** 배포된 URL에서 카카오 로그인 → 4탭 이동이 모바일 브라우저로 동작

---

## P1. 계산 엔진 — ✅ 구현 완료 (2026-07-14) · 상세 플랜: `2026-07-14-p1-engine.md`

**목표:** 만세력·별자리·MBTI·혈액형 계산의 100% 정확한 순수 TS 모듈 (`src/lib/engine/`)
**결과:** 8개 태스크 전부 구현. 대조 코퍼스 21건(년주·오호둔·오서둔 감사). **일주 앵커 완전 확정 (2026-07-16)** — KASI '음양력 정보' 공표 일진 **467건(1900~2050)과 전건 일치**(불일치 0)를 `fixtures/iljin-cases.ts`로 굳혀 오프라인 검증(`iljin.test.ts`). 앵커를 한 칸만 밀어도 467건이 전부 실패한다. 이로써 CLAUDE.md가 말하던 "유일한 외부 가정"이 사라졌다. 절기·DST·표준시 QA는 품질 3·4차에서 완료.

| # | 태스크 | 산출물 |
|---|--------|--------|
| 1 | 천간·지지·오행 기초 타입과 상수 | `engine/types.ts`, `engine/constants.ts` |
| 2 | 절기 데이터 테이블 (연도별 입절 시각) | `engine/solar-terms.ts` |
| 3 | 년주·월주 계산 (절기 기준) + 대조 테스트 | `engine/pillars.ts` |
| 4 | 일주·시주 계산 (자시 23시 경계 포함) + 대조 테스트 | 〃 |
| 5 | 오행 분포·십성 산출 | `engine/elements.ts`, `engine/ten-gods.ts` |
| 6 | 별자리 산출 (12궁 날짜 경계) | `engine/zodiac.ts` |
| 7 | MBTI 16유형·혈액형 4유형 특성 데이터 | `engine/mbti.ts`, `engine/blood.ts` |
| 8 | 통합 `computeProfile()` — 프로필 컨텍스트 JSON 생성 | `engine/index.ts` |

**완료 기준:** 공개 만세력과 대조한 사례 30건 이상 테스트 전부 통과, 경계 사례(자시·절기 경계일·출생시간 미상) 테스트 포함

---

## P2. 온보딩 + 프로필 (🚀 소프트 런칭) — ✅ 완료 (2026-07-15) · 상세 플랜: `2026-07-14-p2-onboarding-profile.md`

**목표:** 가입 → 대화형 온보딩 → "온전한 나" 프로필 공개
**결과:** 8개 태스크 코드 완료 + 마이그레이션 0001 실행(2026-07-15)으로 저장·영속화 활성. 온보딩에 성별(선택) 입력 추가(대운용, 0006). 소셜 로그인은 카카오·구글 모두 프로덕션 동작 확인.

| # | 태스크 | 산출물 |
|---|--------|--------|
| 1 | DB 스키마: `profiles`, `templates`, `interpretations` + RLS | 마이그레이션 |
| 2 | 대화형 온보딩 UI 4단계 (생년월일시→혈액형→MBTI→별자리 확인) | `/onboarding` |
| 3 | 온보딩 제출 → `computeProfile()` → profiles 저장 | API Route |
| 4 | 템플릿 문장 엔진 (조합 키 → 문구 조립) | `src/lib/interpret/templates.ts` |
| 5 | 템플릿 콘텐츠 1차 작성 (일간 10 × 오행 패턴 × MBTI 16 × 혈액형 4 × 별자리 12 조합 문구) | `templates` 시드 데이터 |
| 6 | "온전한 나" 프로필 화면 (공개 연출 포함) | `/me` |
| 7 | 톤 검증 자동 검사 (금지 표현 필터) | `src/lib/interpret/tone-guard.ts` + 테스트 |
| 8 | E2E 테스트 — 온보딩→프로필 공개 흐름 (Playwright) | `e2e/onboarding.spec.ts` |

**완료 기준:** 신규 가입자가 3분 내 프로필을 받아볼 수 있고, 템플릿만으로(LLM 없이) 디테일한 해석이 나옴, E2E 통과

---

## P3. 데일리 가이드 (🚀 정식 출시 v1.0) — ✅ 코드 완료 (2026-07-14)

**목표:** 홈 = 오늘의 가이드, 매일 돌아올 이유 완성
**결과:** 일운 엔진·데일리 콘텐츠·홈 화면 ✅ 배포, interpretations 캐시 + '지난 이야기' 보관함(`0e7c19c`), 마일스톤 배지(`b516c83`)까지 5개 태스크 전부 구현.

| # | 태스크 | 산출물 |
|---|--------|--------|
| 1 | 일운(오늘의 기운) 계산 — 오늘 날짜 간지 × 개인 사주 상호작용 | `engine/daily.ts` + 테스트 |
| 2 | 데일리 해석 생성 + `interpretations` 캐시 (사용자·날짜 키) | API Route |
| 3 | 홈 화면 UI: 기운 한 줄→마음가짐→오늘의 색/키워드→행운 포인트 | `/` |
| 4 | 동행일 카운터 + 마일스톤 배지 (7/30/100/365) | DB 컬럼 + UI |
| 5 | '지난 이야기' 보관함 | `/history` |

**완료 기준:** 자정 이후 첫 방문 시 새 가이드 생성·캐시, 재방문 시 생성 0회

---

## P4. 확산 장치 (v1.1) — ✅ 코드 완료 (2026-07-14) · 상세 플랜: `2026-07-14-p4-growth.md`

**진행:** 공유 카드 라우트+카피(1 ✅), ShareSheet+유입 추적(2 ✅), events 지표+Vercel Analytics(5 ✅), AdFit 슬롯 스텁+채널 문안(3·4 코드 측 ✅). 마이그레이션 0001~0003 실행 완료(2026-07-15) — events 지표 수집 활성. **남은 것: 수동 설정 — 카카오 채널 개설, AdFit 단위 발급, Vercel Analytics 대시보드 활성화**(`2026-07-14-p4-manual-setup.md`).

| # | 태스크 | 산출물 |
|---|--------|--------|
| 1 | 공유 카드 이미지 생성 (9:16, 1:1) — 궁금증 유발형 카피 | `@vercel/og` 이미지 라우트 |
| 2 | 공유 UI (이미지 저장/카카오톡/링크 복사) + 유입 추적 파라미터 | 공유 시트 |
| 3 | 카카오톡 채널 알림 (아침 1회) 또는 PWA 웹 푸시 | 알림 파이프라인 |
| 4 | 광고 부착 (카카오 AdFit) — 가이드 하단 등 비침습 위치 | 광고 슬롯 |
| 5 | 성장 지표 수집 (온보딩 완주율·재방문율·카드 유입) | Vercel Analytics + DB |

**완료 기준:** 공유 카드 경유 신규 가입이 추적 가능

---

## P5. AI 챗 '마음' (v1.2) — ✅ 완료 (2026-07-15) · 상세 플랜: `2026-07-14-p5-chat.md`

**결과:** Provider 인터페이스·템플릿 Provider·Gemini Provider·3단 폴백(1·2·3 ✅ 테스트), 챗 UI·chat_messages·usage_counters·하루10회(4·5 ✅). 마이그레이션 0002 실행 + `GEMINI_API_KEY` 등록으로 LLM 개인화 활성(장애 시 템플릿 폴백). 시스템 프롬프트에 사주 4주·십성·대운 컨텍스트 전달.

| # | 태스크 | 산출물 |
|---|--------|--------|
| 1 | 해석 Provider 인터페이스 (`InterpretProvider`) + 템플릿 Provider 구현 | `src/lib/interpret/provider.ts` |
| 2 | Gemini 무료 티어 Provider (시스템 프롬프트 = §5.4 문체 규칙) | `gemini-provider.ts` |
| 3 | 3단 폴백 체인 (쿼터 소진/오류/톤 위반 → 템플릿) | 폴백 로직 + 테스트 |
| 4 | 챗 UI + `chat_messages` 저장 (동반자의 기억) | `/mind` |
| 5 | 하루 10회 제한 (`usage_counters`) + 희소성 UX 문구 | 제한 미들웨어 |

**완료 기준:** LLM 장애 상황을 강제로 만들어도 챗이 템플릿 응답으로 무중단 동작

---

## P6. 의사결정 도우미 '고민' (v1.3) — ✅ 코드 완료 (2026-07-14) · 상세 플랜: `2026-07-14-p6-p7.md`

**결과:** 고민 탭 전면 구현 — 카테고리 4종(일·커리어/관계/선택·결정/마음) + 자유 서술 → 프로필+오늘 운기 종합 조언. 템플릿이 항상 답하고(톤 검증 통과), LLM이 가능하면 '당신에게' 개인화 문단을 더함. `interpretations(kind=advice)` 보관 + '지난 고민들' 다시 보기, 하루 3회 제한.

| # | 태스크 | 산출물 |
|---|--------|--------|
| 1 | 고민 입력 UI (카테고리: 커리어/관계/선택 + 자유 서술) | `/concern` ✅ (`ConcernRoom`) |
| 2 | 조언 생성 — 프로필 + 현재 운기 + 고민 맥락 종합 | `content/concern.ts` + `concern/actions.ts` ✅ |
| 3 | 조언 기록 보관 + 다시 보기 | `interpretations(kind=advice)` ✅ |

**완료 기준:** 고민 입력 → 프로필·운기가 반영된 맞춤 조언 (톤 검증 통과) — 코드·테스트 충족

---

## P7. 성장 (v2.0) — ✅ 완료 (2026-07-16) · 상세 플랜: `2026-07-14-p6-p7.md`

**진행:** 입력형 궁합 ✅ — `engine/match.ts`(오행 관계·천간합·육합·충·별자리 4원소·MBTI 어울림, TDD, 점수 대칭) + `content/match.ts`(관계 해석, 톤 통과) + `/match`(연인/친구/동료 모드). 양방향 심층 궁합 ✅ — `connections`(0005, 토큰 초대장·프로필 스냅샷·RLS) + `computeDeepMatch`(오행 상호 보완 보너스) + `/connect/[token]`(열람→수락→심층 결과). 프리미엄 게이트 ✅ — `premium_until`(0004) + 챗 무제한 게이트 + 소진 화면 판매 포인트. **마이그레이션 0004·0005 실행 완료(2026-07-15) — 두 기능 모두 프로덕션 활성.** 결제 연동(토스페이먼츠) 코드 완료 — 30일 이용권 단건 결제(`/premium` → 토스 결제창 → `/premium/success` 서버 승인 → `premium_until` 연장). 금액 진실은 서버 `payments` 행, 재승인 멱등, `premium_until`은 컬럼 GRANT로 잠가 service role 경로만 갱신 가능(0007). 토스 테스트 키·service role 키 발급 및 로컬/Vercel env 등록 완료, 로컬 테스트 결제 통과(2026-07-16). 머지 전 코드리뷰(high)로 확정 결함 6건 수정 — grant 목록 user_id 누락(온보딩 upsert 차단), 부여 로직을 `grant_premium_for_order`(행 락, 멱등, 원자 연장) SQL 함수로 이관, ALREADY_PROCESSED 자가 복구, done 행 보호, NaN 날짜 가드, fail 페이지 외부 문자열 반영 제거. 0007 개정판 재실행 완료 + main 머지(`b26e015`)·배포 후 **프로덕션 테스트 결제 확인(2026-07-16) — P7 완료.** 실 판매 시점 과제: PG 계약 후 라이브 키 교체, 유료 LLM Provider 어댑터(프리미엄도 현재 Gemini 무료 경로).

| # | 태스크 | 산출물 |
|---|--------|--------|
| 1 | 궁합 "우리의 조합" (상대 정보 입력형) | `/match` ✅ |
| 2 | 초대 연결 시 양방향 심층 궁합 해제 | `/connect/[token]` + `connections`(0005) ✅ |
| 3 | 프리미엄 구독 (결제 연동, 챗 무제한, 유료 LLM Provider 어댑터) | 무제한 게이트 ✅(레거시, P8에서 `src/lib/consult/quota.ts`로 이관), 결제 ✅(토스 30일 이용권, `/premium`+0007), 유료 LLM 어댑터는 P8에서 구현 |

**완료 기준:** 무료/프리미엄 이중 운영 — 구독자는 유료 모델, 무료 사용자는 기존 경로

---

## P8. 3단계 요금제 + 로그 관리 (v2.1) — ✅ 코드 완료 (2026-07-17)

**목표:** P7의 "30일 이용권 무제한" 단일 구독 모델을, 무료(비로그인)→로그인(하루 1회 무료)→구독(횟수제 크레딧) 3단계로 재편하고, 마음/고민 대화 기록을 사용자가 직접 정리할 수 있게 한다. `feat/service-tiers-log-delete` 브랜치에서 작업, 마이그레이션 0008.

**단계 정의(사용자 확정):**
- **1단계(비로그인)** — 프로필+데일리까지만. 실제로는 이미 게이트가 없었다(`computeProfile`/`computeDaily`가 순수 함수라 온보딩 리빌·홈 데일리가 로그인 없이 동작 중). 마음·고민 상담은 로그인 필수(기존과 동일, 액션 레벨 `auth` 체크).
- **2단계(로그인)** — 마음·고민 **각각 하루 1회 무료**(기존 챗 10회/고민 3회에서 축소). 데일리는 여전히 100% 템플릿(LLM 미사용 — `daily/actions.ts` 확인 결과 원래도 그랬다)이라 "더 전문적"인 차별점은 상담 크레딧·유료 모델 자체에 있다.
- **3단계(구독)** — 무료 슬롯을 넘기면 `profiles.consult_credits`(공유 풀, 마음·고민 공통)를 소비. 패키지 5회 5,000원 / 10회 10,000원(1회당 1,000원), 토스페이먼츠 흐름 재사용(`payment/credits-actions.ts`). 크레딧 소비 상담은 유료 모델(`OpenRouterProvider({ premium: true })`, 기본 `anthropic/claude-3.5-haiku`)로 응답 — 더 길고(4~7문장) 구체적인 시스템 프롬프트.
- **레거시 호환** — 기존 30일 이용권(`premium_until`) 구매자는 만료 전까지 마음·고민 모두 무제한 유지(`consultAccess()`가 최우선으로 체크). 신규 판매는 크레딧 방식만.

**구현:**
- `src/lib/consult/quota.ts` — 챗·고민이 공유하는 접근 판정(`consultAccess`) 한 곳. 기존 `chat/quota.ts`·`concern/constants.ts`의 `CONCERN_DAILY_LIMIT`을 대체.
- `0008_p8_tiers.sql` — `profiles.consult_credits`, `chat_messages`/`interpretations(kind=advice)` 삭제 RLS, `consume_consult_credit()`(본인 크레딧 원자적 차감, authenticated 직접 실행 가능·auth.uid() 자체 스코프), `grant_credits_for_order()`(결제 승인 후 부여, service role 전용 — `grant_premium_for_order`와 동일 패턴), `payments.kind/credits/granted_credits`.
- `chat/actions.ts`·`concern/actions.ts` — `deleteChatMessage`/`deleteAllChatMessages`, `deleteConcernLog`/`deleteAllConcernLogs` 추가. `submitConcern`이 실제 DB id를 반환하도록 수정(기존엔 로컬 히스토리에 가짜 id를 써서 방금 만든 항목을 삭제할 수 없었다).
- `MindChat.tsx`/`ConcernRoom.tsx` — 메시지·기록 개별 삭제(hover 시 ✕ 버튼) + 헤더 "전체 삭제"(확인 다이얼로그).
- `/premium` — 3단계 안내 + 레거시 이용권 잔여 기간 표시(있으면) + 크레딧 잔액 + 패키지 구매 버튼. 결제 성공/실패 라우트는 `/premium/credits/success`·`/fail`로 신설(기존 `/premium/success`·`/fail`·`PayButton.tsx`·`payment/actions.ts`는 레거시 이용권 구매자 대응용으로 코드는 남기되 UI에서는 더 이상 연결하지 않음).

**완료 기준:** 비로그인은 프로필·데일리만, 로그인은 상담 하루 1회 무료, 그 이상은 크레딧 구매로 유료 모델 상담. 마음·고민 로그를 개별·전체 삭제 가능. `npm run verify`(lint·typecheck·test 350건·build) 통과.

**추가 — 2단계(로그인) 전문성 실질 차별화 (사용자 피드백 반영, 2차 수정):** 1차 시도는 비로그인 온보딩 리빌을 요약 카드(`SajuTeaser`)+섹션 잠금으로 축소해 차별화했으나, "1단계 데일리는 템플릿이라 좋지만 프로필까지 요약하지는 말라"는 피드백을 받고 되돌렸다 — **비로그인도 사주 명식 전체(`SajuChart`)와 해석 7섹션 전체를 그대로 본다**(`SajuTeaser.tsx` 삭제, 온보딩 `ProfileView`를 잠금 이전 상태로 복원). 대신 2단계 차별화는 "접근 제한"이 아니라 "로그인 전용으로 실제로 더 깊은 콘텐츠를 새로 만든다" 방향으로:
- **데일리 가이드 LLM 개인화** — 데일리는 원래 100% 템플릿(로그인 여부 무관 동일)이었다. `content/daily.ts`의 `dailyPrompt()` + `daily/actions.ts`의 `recordTodayDaily()`(로그인 전용, 하루 1회 캐시)가 무료 OpenRouter 모델로 "오늘, 당신만을 위한 이야기" 문단을 1회 시도 생성 — 실패해도 템플릿은 그대로 캐시. 홈 화면이 오늘자 캐시를 읽어 있으면 보여준다(당일 첫 방문엔 캐시가 없어 다음 방문부터 보임).
- **프로필 심층 리포트(신규, 3차 확장)** — "혈액형·MBTI·별자리도 요약이 아니라 각 체계에 이미 정의된 전체 서술을 사주 데이터와 섞어달라", 이어서 "나열 말고 옴니마인드 가치 기반으로 현재·미래 이야기 예측, 성격·취향·좋아하는 색 등 퍼스널 분석, 연애운·사업운·관계운·금전운까지 전부 자세히"라는 두 차례 피드백에 따라 최종적으로:
  - `content/synthesis.ts`의 `profileSynthesisPrompt()`가 유형 라벨("ENFJ", "O형")이 아니라 **각 체계가 이미 갖고 있는 완전한 결 서술**(`DAY_MASTER_TEXT`·`ELEMENT_BALANCE_TEXT`·`tenGodTheme`·`MBTI_AXIS_TEXT`·`BLOOD_TEXT`·`ZODIAC_TEXT`)을 프롬프트에 그대로 실어 LLM에 넘긴다.
  - `chat-prompt.ts`의 `chatSystemPrompt(..., {report:true})`가 옴니마인드 가치("데이터를 딱딱한 분석이 아니라 따뜻한 발견과 공감으로", "운명을 단정짓는 점술이 아니에요")를 명시하고, **[성격과 취향] [당신의 색] [지금과 앞으로] [연애운] [사업운·커리어] [관계운] [금전운]** 7개 항목을 대괄호 제목 형식으로 순서까지 지시한다.
  - `OpenRouterProvider`에 `report` 모드(무료 모델 그대로, 응답 예산만 300→1200 토큰, temperature 0.85)를 추가 — 7개 항목을 자세히 쓰기엔 챗용 300토큰이 턱없이 부족했다.
  - `content/synthesis.ts`의 `parseReportSections()`가 "[제목]\n내용" 텍스트를 `InterpretationSection[]`으로 분리 — 대괄호 제목을 하나도 못 찾으면(LLM이 형식을 안 지켜도) 전체를 한 섹션으로 안전하게 폴백한다.
  - `onboarding/actions.ts`의 `saveProfile()`(로그인 전용)이 report 모드로 1회 시도해, 성공하면 파싱된 최대 7개 섹션을 템플릿 7섹션에 이어 `interpretations(kind=profile)`에 함께 캐시한다. `/me`는 이미 이 캐시를 그대로 렌더링하므로 추가 배선이 필요 없다.
  - `share/card-copy.ts`의 `PROFILE_MAX_SECTIONS`를 10→16으로 올려(최대 7+7=14섹션 수용) 카드 이미지 생성이 새 섹션들을 자르지 않게 했다.
- 결과: 1단계(비로그인)=사주 명식 전체+해석 7섹션+템플릿 데일리(저장·기록 없음), 2단계(로그인)=1단계와 같은 콘텐츠 + LLM 심층 리포트(성격·취향·색·현재/미래·연애운·사업운·관계운·금전운, 최대 7섹션) + LLM 개인화 데일리 + 저장/기록, 3단계(구독)=그 이상 상담(마음·고민) 크레딧+유료 모델.

**잔여(다음 기회):**
- ~~마이그레이션 0008 Supabase 실행~~ — 2026-07-21 실행 확인(`profiles.consult_credits`,
  `payments.kind/credits/granted_credits` 컬럼 존재를 REST API로 직접 대조).
- 실 판매 확인 — 크레딧 패키지 결제도 P7처럼 프로덕션 테스트 결제로 실측 검증 필요.
- `OPENROUTER_PREMIUM_MODEL` 미설정 시 `anthropic/claude-3.5-haiku` 기본값 사용 — 실제 비용·품질은 아직 실측 전(P7 무료 모델 선정 때처럼 실 키로 비교 검증 권장).

---

## 콘텐츠 전문성 개선 — ✅ 1·2차 완료 (2026-07-15)

전문성 검토(10개 항목)에 따른 해석 콘텐츠·엔진 개선. 로드맵 단계와 별도로 수행.

- **1차 (`f6e516e`)** — 템플릿 챗 이중 주어 비문 수정, 데일리 무드 천간 10종 + 십성 10갈래 개인화, 궁합 점수 대칭화, 천간합·육합·충 판정(`computeBond`) 반영, 프로필 '조각이 만나는 자리' 종합 섹션(십성×MBTI E/I 교차 10종) 신설.
- **2차 (`27274d4`, `a18047b`)** — 명식표 지장간(여기·중기·정기) 표기, **대운 엔진**(`engine/daeun.ts`, 순행/역행·대운수·TDD) + `/me` "운의 계절" 카드 + 온보딩 성별(선택) 입력(0006), 오행 dominant 득령 가중·동률 병기, 궁합 온도 구간 재조정(65/78/90), 톤 가드 `~니다` 일반화·단정 어미 규칙 + 일간 10종·별자리 12종 재작성, 고민 방향 제안 카테고리×십성 20종, 챗 LLM 프로필 컨텍스트 확장, 홈 로그아웃·페이드 전환.
- **Google 로그인** (`e9c75a9`, `ea8025c`) — 공식 로고·오류 안내·실패 사유 표시. Google Cloud redirect URI 등록 + Client Secret 교체로 프로덕션 동작 확인(2026-07-15).
- **마이그레이션 0001~0006 전부 적용 완료** (2026-07-15). 대운수는 실사용 교차 확인 완료.
- **품질 3차 (2026-07-16)** — DST 1948~60 구간 확정·추가(tzdata ROK 기준, 경계 테스트 13건), 12운성 엔진(`engine/twelve-stages.ts`, 순행/역행 TDD) + 명식표 표기, **KASI 절기 분단위 보정 실행 완료** — `scripts/verify-solar-terms.ts`로 API 커버 범위(2000~2028) 전체 659건 패치, 월주 대조 테스트 전부 유지. 커버 밖 연도(1900~1999·2029+)는 Meeus 근사 유지. 머지 전 리뷰(high)로 강화: **KASI 원본 자체의 오류 3건(2011 대한 +1일·2011 입동 +356분·2019 대한 "17:60") 발견 → ±30분 초과·불법 시각 거부 가드로 차단**(최종 656건 반영), 절기표 무결성 테스트(전 연도 유효 시각·단조 증가) 추가, **시간미상×DST 출생의 일주가 전날로 밀리던 버그 수정**(날짜를 입력 출생일 기준으로 고정). 수용 리스크: 분 단위 보정으로 절입 경계 수 분 창의 기존 저장 프로필은 신규 계산과 다를 수 있음 — 재온보딩 시 재계산되며 현 사용자 규모에서 영향 극미.
- **품질 4차 (2026-07-16)** — 남은 시간 정밀도 과제 2건 해결.
  - **표준시 UTC+8:30 보정** — `engine/clock.ts` 신설(`toTrueInstant`): 기록 벽시계 → 실제 절대 시각을 한 곳에서 책임진다. 시계가 동경 127.5°(UTC+8:30)였던 **1954-03-21~1961-08-09**와 **1908-04-01~1911-12-31** 구간에 +30분, 서머타임에 −1h를 적용하며, 두 보정의 판정 기준은 항상 '기록된 벽시계'라 순서에 무관하다(1955~60년 여름은 두 구간에 동시 소속 → 순 −30분). 30분은 시주 경계(2시간)와 야자시 경계(23시)를 실제로 넘기므로 테스트로 고정. 함께 발견·수정: **대운이 보정 안 된 시각을 받던 불일치**(`resolveBirthInstant`로 4주·대운이 같은 시각을 공유), 시간 미상 시 자정 대신 정오 사용(daeun.ts 문서주석이 원래 요구하던 계약).
  - **절기 정밀 재계산** — 생성기를 Meeus 저차 근사 → **astronomy-engine**(태양 겉보기 황경)으로 교체하고 **초 단위**로 저장(분 반올림 시 절입과 같은 분에 태어난 사람의 월주가 반올림 방향으로 뒤집힘). 정확도는 KASI와 독립된 **USNO 공표 분점·지점(2016~2036, 84건)** 대조 테스트로 상시 검증(`solar-terms.usno.test.ts`). 이로써 **KASI 커버 밖이라 손대지 못했던 1900~1999년의 최대 ±12분 오차가 해소**됐다.
  - **KASI의 역할 강등** — API 원본에 오류가 확인되어(2015 하지 +20분, 2011 대한 +1일, 2011 입동 +351분, 2019 대한 "17:60") `verify-solar-terms.ts`에서 **패치 기능(--write)을 제거**하고 감시 도구로 전환. 대조 결과 694건 중 685건(98.7%)이 1분 이내 일치, 나머지는 위 오류 3건과 1.1분 표기차뿐. 2015 하지 오류값은 품질 3차 때 ±30분 가드를 통과해 main에 들어갔었고 이번 재생성으로 제거됐다.
  - **머지 전 리뷰(high) 반영** — 대운 정합 수정에 회귀 테스트 신설(보정을 빼면 대운수가 갈리는 실제 사례로 고정: 1954~61 구간 135건·시간미상 3년치 305건을 탐색해 3건을 대표로 채택), `PROFILE_CONTEXT_VERSION`(1→2) 도입으로 낡은 저장 프로필을 식별 가능하게, `kstStringToInstant`·`birthTime` 값 범위 검증(전엔 "99:99"가 Invalid Date로 흘러 절기 테이블에서 엉뚱하게 터졌다), 감시 스크립트의 거짓 all-clear 경로 제거(인자 미검증·NaN 대조·0건 대조를 전부 실패 처리, 확인된 KASI 오류 4건은 `KNOWN_KASI_DEFECTS`로 등록해 '새 이상'만 실패), `dst.ts`를 `clock.ts`로 통합(중복 제거·낡은 주석 정리), CLAUDE.md 아키텍처 현행화.
- 잔여(다음 기회):
  - **저장 프로필 재계산(version 1 → 2)** — 이번 변경으로 기존 저장값과 엔진 계산이 갈린다. 입력(생년월일시·성별)이 모두 남아 있어 재계산은 가능하나, 해석 캐시(`interpretations`)까지 함께 무효화해야 해 별도 작업으로 둔다. 현 사용자 규모에선 재온보딩으로도 해소된다.
  - **1954~61 구간 만세력 대조 케이스** — 30분 보정은 tzdata 사실 + 동경135° 기준(이 엔진의 일관된 규약)에서 도출했고 자기정합 테스트로 고정했지만, `fixtures/manseryeok-cases.ts`에 해당 구간 외부 대조 사례가 아직 없다(최고(最古) 1976-08-15). 실제 1954~61년생 사례를 외부 만세력과 대조해 추가하면 가정이 사실로 굳는다.
  - 1908-04-01 이전 지방평균시(+8:27:52) 구간(서비스 대상 밖이라 미보정), 온도 죽은 구간 외 세부 다듬기.

---

## 단계 전환 규칙

1. 각 단계 완료 기준 충족 → 배포 → 다음 단계 상세 플랜 작성
2. 상세 플랜은 이 로드맵의 태스크 분해를 기준으로 하되, 직전 단계의 실제 코드에 맞춰 조정
3. 로드맵 자체의 변경(태스크 추가/삭제)은 이 파일을 갱신하고 커밋
