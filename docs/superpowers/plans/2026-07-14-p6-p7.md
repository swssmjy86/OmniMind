# P6 의사결정 도우미 + P7 성장 구현 플랜 (2026-07-14)

> 로드맵: `2026-07-13-omnimind-roadmap.md` · 설계서: `docs/superpowers/specs/2026-07-13-omnimind-design.md`

## P6. '고민' 탭 — ✅ 코드 완료

**흐름:** 카테고리 선택 + 자유 서술 → `submitConcern()` → 템플릿 조언(항상) + LLM 개인화(가능할 때) → `interpretations(kind=advice)` 보관 → '지난 고민들' 다시 보기.

| 모듈 | 역할 |
|------|------|
| `src/lib/interpret/content/concern.ts` | 카테고리 4종(일·커리어/관계/선택·결정/마음) × 오행 관계 5종 조언 조립 + LLM 프롬프트. 전 조합 톤 검증 테스트 |
| `src/lib/concern/actions.ts` | 서버 액션 — 하루 3회 제한(advice 행 created_at, KST 자정 기준), 3단 폴백 재사용(`respond()` 주입) |
| `src/lib/concern/constants.ts` | `CONCERN_DAILY_LIMIT=3`, 서술 500자 |
| `src/components/concern/ConcernRoom.tsx` | 카테고리 칩 + 입력 + 결과 카드 + 지난 고민(details) |
| `src/app/(tabs)/concern/page.tsx` | 프로필 없으면 온보딩 CTA, 있으면 ConcernRoom |

**설계 결정:** `interpretations`의 `unique(user_id,kind,target_date)`는 advice에서 `target_date=null`로 우회(Postgres에서 null은 중복 허용) — 마이그레이션 없이 하루 여러 건 보관. LLM 실패 시 개인화 문단만 조용히 생략(무중단 원칙 §8).

## P7. 성장 — 🟡 착수

### P7-1 궁합 "우리의 조합" ✅

**흐름:** 내 프로필(일간 오행·별자리·MBTI) × 상대(생년월일 + MBTI 선택) → 순수 계산 → 해석 카드 → 초대 링크.

| 모듈 | 역할 |
|------|------|
| `src/lib/engine/match.ts` | TDD. `partnerFromBirth`(정오 기준 일주→오행, 별자리), `relateElement` 재사용(동행/채움/발산/결실/단련), 별자리 4원소 조화(닮음/어울림/다름), `mbtiSynergy` 0~5, 모드 가중 점수 0~100(결정적) |
| `src/lib/interpret/content/match.ts` | "우리의 온도 N°" + 관계 해석 5종 + 원소 조화 3종 + 성향 3단 + 모드 마무리. 서열화·공포 금지, 전 조합 톤 통과 |
| `src/components/match/MatchForm.tsx` | 모드 칩(연인/친구/동료) + 입력 → 클라이언트 계산(엔진 순수함수) + 초대 링크 복사(`?ref=match&via=<slug>`) |
| `src/app/(tabs)/match/page.tsx` | 진입: `/me`의 "우리의 조합 보기" 버튼 |
| `src/lib/metrics/names.ts` | `match_compute`, `match_copy_link` 이벤트 추가 |

### P7-2 양방향 심층 궁합 ✅

**흐름:** `/match`에서 "상대에게 건네주기" → `createInvite()`가 토큰 초대장 발급(내 프로필 스냅샷 포함) → 상대가 `/connect/<token>` 열람 → (프로필 없으면 온보딩 후 재방문) → 수락 → 두 사주 전체로 심층 해석. 결과는 두 당사자만 열람.

| 모듈 | 역할 |
|------|------|
| `supabase/migrations/0005_p7_connections.sql` | `connections` — 토큰·모드(슬러그)·양측 닉네임/프로필 스냅샷·상태. RLS: pending은 토큰 소지자 열람, accepted는 당사자만, 수락은 초대자 아닌 로그인 사용자가 자신을 invitee로 채울 때만 |
| `engine/match.ts` `computeDeepMatch` | 두 ProfileContext → 입력형과 같은 점수 체계 + **오행 상호 보완 보너스**(서로의 lacking을 채우는 오행, +4/개 최대 +12) — 초대 연결의 보상 |
| `content/match.ts` `assembleDeepMatch` | 6개 섹션 — "서로를 채우는 조각"(보완 4갈래 문구) 추가, 전 조합 톤 통과 |
| `src/lib/match/actions.ts` | `createInvite`(스냅샷 insert + 지표), `acceptInvite`(RLS 조건 하 수락 + 지표) |
| `src/app/(tabs)/connect/[token]/page.tsx` | 초대장 상태별 화면(없음/대기/수락 유도/심층 결과) |
| `/match` | "이어진 조합" 목록(대기 중/수락됨) |

**설계 결정:** 프로필을 connections 행에 스냅샷으로 저장해 profiles의 본인-전용 RLS를 유지(상대 프로필 직접 조회 불필요). 계산은 열람 시점에 순수 함수로 — 캐시 불필요(LLM 미사용).

### P7-3 프리미엄 — 게이트 준비 ✅ / 결제 미착수

| 모듈 | 역할 |
|------|------|
| `supabase/migrations/0004_p7_premium.sql` | `profiles.premium_until timestamptz` — 결제 전엔 SQL 수동 부여 |
| `src/lib/chat/quota.ts` | `isPremium`, `chatRemaining`(프리미엄=UNLIMITED 센티널 -1) + 테스트 |
| `src/lib/chat/actions.ts` · `mind/page.tsx` | 프리미엄이면 하루 10회 제한 통과 |
| `MindChat.tsx` | 무제한 헤더("마음껏 나눌 수 있어요 ✨") + 소진 화면 판매 포인트 문구(§6.4) |

유료 LLM 어댑터는 `InterpretProvider` 인터페이스 뒤에 추가만 하면 됨(설계 §2 충족, 코드 변경 불필요 구조).

## 수동 작업 (사용자)

- Supabase SQL Editor에서 `0004_p7_premium.sql`, `0005_p7_connections.sql` 실행 (`2026-07-14-p4-manual-setup.md` 체크리스트에 추가됨)
- 프리미엄 수동 부여 예: `update public.profiles set premium_until = now() + interval '30 days' where user_id = '<uuid>';`

## 남은 것 (P7 완료 기준까지)

- 결제 연동 — PG(토스페이먼츠 등) 가입·키 발급이 선행돼야 하는 외부 작업. 코드 측은 `premium_until` 갱신 웹훅 + 구독 페이지만 추가하면 됨
- 유료 LLM 어댑터 — 수익 발생 시 `InterpretProvider` 구현체 1개 추가
